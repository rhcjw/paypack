[project]
name = "paypack"
version = "0.1.0"
description = "AI Agent universal payment middleware — USDC/ETH + Alipay/WeChatPay CNY"
requires-python = ">=3.12"
dependencies = [
    "langchain-paypack>=0.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
